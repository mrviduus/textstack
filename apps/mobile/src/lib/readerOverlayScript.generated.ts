// AUTO-GENERATED — do not edit.
// Source: packages/reader-overlay/src/mobileBootstrap.ts
// Regenerate: pnpm -C apps/web build:mobile-overlay
//
// IIFE bundle of the shared @textstack/reader-overlay package, transpiled
// for Android WebView (es2017). Injected into the WebView by readerHtml.ts.

/* eslint-disable */
/* prettier-ignore */
export const READER_OVERLAY_SCRIPT = "\n(() => {\n  var __defProp = Object.defineProperty;\n  var __typeError = (msg) => {\n    throw TypeError(msg);\n  };\n  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;\n  var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== \"symbol\" ? key + \"\" : key, value);\n  var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError(\"Cannot \" + msg);\n  var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, \"read from private field\"), getter ? getter.call(obj) : member.get(obj));\n  var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError(\"Cannot add the same private member more than once\") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);\n  var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, \"write to private field\"), setter ? setter.call(obj, value) : member.set(obj, value), value);\n  var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, \"access private method\"), method);\n\n  // ../../packages/reader-overlay/src/readerOverlay.ts\n  var SVG_NS = \"http://www.w3.org/2000/svg\";\n  var createSVGElement = (tag) => document.createElementNS(SVG_NS, tag);\n  function toDocRect(r, sx, sy) {\n    return {\n      x: r.x + sx,\n      y: r.y + sy,\n      left: r.left + sx,\n      top: r.top + sy,\n      right: r.right + sx,\n      bottom: r.bottom + sy,\n      width: r.width,\n      height: r.height,\n      toJSON: () => ({})\n    };\n  }\n  function uncollapseForMeasure(range) {\n    if (!range.collapsed) return range;\n    const { endContainer } = range;\n    if (endContainer.nodeType === Node.ELEMENT_NODE) return range;\n    const clone = range.cloneRange();\n    const endOffset = clone.endOffset;\n    const len = endContainer.length;\n    if (endOffset + 1 <= len) clone.setEnd(endContainer, endOffset + 1);\n    else if (endOffset >= 1) clone.setStart(endContainer, endOffset - 1);\n    else if (endContainer.parentNode) clone.selectNode(endContainer.parentNode);\n    return clone;\n  }\n  function captureRects(range) {\n    const sx = typeof window !== \"undefined\" ? window.scrollX : 0;\n    const sy = typeof window !== \"undefined\" ? window.scrollY : 0;\n    let raw = Array.from(range.getClientRects());\n    if (raw.length === 0) {\n      const fallback = uncollapseForMeasure(range);\n      if (fallback !== range) raw = Array.from(fallback.getClientRects());\n    }\n    return raw.filter((r) => r.width > 0 && r.height > 0).map((r) => toDocRect(r, sx, sy));\n  }\n  function nowMs() {\n    return typeof performance !== \"undefined\" && typeof performance.now === \"function\" ? performance.now() : Date.now();\n  }\n  var _svg, _map, _onMiss, _onRedraw, _justAnchoredUntil, _Overlayer_instances, reportMiss_fn;\n  var Overlayer = class {\n    constructor(options = {}) {\n      __privateAdd(this, _Overlayer_instances);\n      __privateAdd(this, _svg);\n      __privateAdd(this, _map, /* @__PURE__ */ new Map());\n      __privateAdd(this, _onMiss);\n      __privateAdd(this, _onRedraw);\n      // Port of foliate-js/view.js `justAnchored`. On annotation tap we mark a\n      // short \"cooldown\" window; any selectionchange or synthetic click (iOS\n      // Safari fires one ~300 ms after touchend) during it is ignored by the\n      // consumer. Eliminates the 50/50 hit-test-vs-selection-popup race.\n      __privateAdd(this, _justAnchoredUntil, 0);\n      __privateSet(this, _svg, createSVGElement(\"svg\"));\n      __privateGet(this, _svg).setAttribute(\"data-reader-overlay\", \"true\");\n      Object.assign(__privateGet(this, _svg).style, {\n        position: \"absolute\",\n        top: \"0\",\n        left: \"0\",\n        width: \"100%\",\n        height: \"100%\",\n        pointerEvents: \"none\",\n        // UA default for outer <svg> is overflow:hidden — clips any rect whose\n        // internal y exceeds svg.height. Our rects are stored in DOCUMENT coords\n        // and rendered via transform:translate(-scrollX,-scrollY), so anything\n        // past the first viewport (y > h) gets silently clipped → vocab/highlight\n        // underlines vanish after scroll.\n        overflow: \"visible\"\n      });\n      __privateSet(this, _onMiss, options.onMiss);\n      __privateSet(this, _onRedraw, options.onRedraw);\n      this.syncScroll();\n    }\n    // Open the cooldown window. Default 400 ms covers iOS Safari's 300 ms\n    // synthetic-mouse replay plus a small slop for slow event loops.\n    markJustAnchored(ms = 400) {\n      __privateSet(this, _justAnchoredUntil, nowMs() + ms);\n    }\n    isJustAnchored() {\n      return nowMs() < __privateGet(this, _justAnchoredUntil);\n    }\n    get element() {\n      return __privateGet(this, _svg);\n    }\n    get size() {\n      return __privateGet(this, _map).size;\n    }\n    has(key) {\n      return __privateGet(this, _map).has(key);\n    }\n    // Counter-translate the SVG by current scroll so document-coord rects\n    // inside it land on screen where the text actually is. O(1) vs a full\n    // redraw, so safe to call on every scroll frame.\n    syncScroll() {\n      const sx = typeof window !== \"undefined\" ? window.scrollX : 0;\n      const sy = typeof window !== \"undefined\" ? window.scrollY : 0;\n      __privateGet(this, _svg).style.transform = `translate(${-sx}px, ${-sy}px)`;\n    }\n    add(key, range, draw, options) {\n      if (__privateGet(this, _map).has(key)) this.remove(key);\n      const resolved = typeof range === \"function\" ? range(__privateGet(this, _svg).getRootNode()) : range;\n      if (!resolved) return;\n      const rects = captureRects(resolved);\n      if (rects.length === 0) __privateMethod(this, _Overlayer_instances, reportMiss_fn).call(this, key, \"empty-rects\");\n      const element = draw(rects, options);\n      __privateGet(this, _svg).append(element);\n      __privateGet(this, _map).set(key, { range: resolved, draw, options, element, rects });\n      this.syncScroll();\n    }\n    remove(key) {\n      const entry = __privateGet(this, _map).get(key);\n      if (!entry) return;\n      if (entry.element.parentNode === __privateGet(this, _svg)) {\n        __privateGet(this, _svg).removeChild(entry.element);\n      }\n      __privateGet(this, _map).delete(key);\n    }\n    clear() {\n      for (const key of Array.from(__privateGet(this, _map).keys())) this.remove(key);\n    }\n    redraw() {\n      let missCount = 0;\n      for (const [key, entry] of __privateGet(this, _map).entries()) {\n        const { range, draw, options, element } = entry;\n        if (element.parentNode === __privateGet(this, _svg)) __privateGet(this, _svg).removeChild(element);\n        const rects = captureRects(range);\n        if (rects.length === 0) {\n          missCount++;\n          __privateMethod(this, _Overlayer_instances, reportMiss_fn).call(this, key, \"empty-rects-redraw\");\n        }\n        const next = draw(rects, options);\n        __privateGet(this, _svg).append(next);\n        entry.element = next;\n        entry.rects = rects;\n      }\n      this.syncScroll();\n      if (__privateGet(this, _onRedraw)) {\n        try {\n          __privateGet(this, _onRedraw).call(this, { size: __privateGet(this, _map).size, missCount });\n        } catch (e) {\n        }\n      }\n    }\n    hitTest(point) {\n      const sx = typeof window !== \"undefined\" ? window.scrollX : 0;\n      const sy = typeof window !== \"undefined\" ? window.scrollY : 0;\n      const px = point.x + sx;\n      const py = point.y + sy;\n      const entries = Array.from(__privateGet(this, _map).entries());\n      for (let i = entries.length - 1; i >= 0; i--) {\n        const [key, obj] = entries[i];\n        for (const rect of obj.rects) {\n          if (rect.top <= py && rect.left <= px && rect.bottom > py && rect.right > px) {\n            return [key, obj.range];\n          }\n        }\n      }\n      return [];\n    }\n    keys() {\n      return __privateGet(this, _map).keys();\n    }\n  };\n  _svg = new WeakMap();\n  _map = new WeakMap();\n  _onMiss = new WeakMap();\n  _onRedraw = new WeakMap();\n  _justAnchoredUntil = new WeakMap();\n  _Overlayer_instances = new WeakSet();\n  reportMiss_fn = function(key, reason) {\n    if (!__privateGet(this, _onMiss)) return;\n    try {\n      __privateGet(this, _onMiss).call(this, key, reason);\n    } catch (e) {\n    }\n  };\n  // --- Draw palette ---\n  __publicField(Overlayer, \"highlight\", (rects, options = {}) => {\n    const { color = \"yellow\", opacity = 0.3, blendMode = \"multiply\" } = options;\n    const g = createSVGElement(\"g\");\n    g.style.fill = color;\n    g.style.opacity = `var(--overlay-highlight-opacity, ${opacity})`;\n    g.style.mixBlendMode = `var(--overlay-highlight-blend-mode, ${blendMode})`;\n    for (const { left, top, height, width } of rects) {\n      const el = createSVGElement(\"rect\");\n      el.setAttribute(\"x\", String(left));\n      el.setAttribute(\"y\", String(top));\n      el.setAttribute(\"height\", String(height));\n      el.setAttribute(\"width\", String(width));\n      g.append(el);\n    }\n    return g;\n  });\n  __publicField(Overlayer, \"underline\", (rects, options = {}) => {\n    const { color = \"red\", width: strokeWidth = 2, writingMode } = options;\n    const g = createSVGElement(\"g\");\n    g.style.fill = color;\n    if (writingMode === \"vertical-rl\" || writingMode === \"vertical-lr\") {\n      for (const { right, top, height } of rects) {\n        const el = createSVGElement(\"rect\");\n        el.setAttribute(\"x\", String(right - strokeWidth));\n        el.setAttribute(\"y\", String(top));\n        el.setAttribute(\"height\", String(height));\n        el.setAttribute(\"width\", String(strokeWidth));\n        g.append(el);\n      }\n    } else {\n      for (const { left, bottom, width } of rects) {\n        const el = createSVGElement(\"rect\");\n        el.setAttribute(\"x\", String(left));\n        el.setAttribute(\"y\", String(bottom - strokeWidth));\n        el.setAttribute(\"height\", String(strokeWidth));\n        el.setAttribute(\"width\", String(width));\n        g.append(el);\n      }\n    }\n    return g;\n  });\n  __publicField(Overlayer, \"strikethrough\", (rects, options = {}) => {\n    const { color = \"red\", width: strokeWidth = 2, writingMode } = options;\n    const g = createSVGElement(\"g\");\n    g.style.fill = color;\n    if (writingMode === \"vertical-rl\" || writingMode === \"vertical-lr\") {\n      for (const { right, left, top, height } of rects) {\n        const el = createSVGElement(\"rect\");\n        el.setAttribute(\"x\", String((right + left) / 2));\n        el.setAttribute(\"y\", String(top));\n        el.setAttribute(\"height\", String(height));\n        el.setAttribute(\"width\", String(strokeWidth));\n        g.append(el);\n      }\n    } else {\n      for (const { left, top, bottom, width } of rects) {\n        const el = createSVGElement(\"rect\");\n        el.setAttribute(\"x\", String(left));\n        el.setAttribute(\"y\", String((top + bottom) / 2));\n        el.setAttribute(\"height\", String(strokeWidth));\n        el.setAttribute(\"width\", String(width));\n        g.append(el);\n      }\n    }\n    return g;\n  });\n  __publicField(Overlayer, \"squiggly\", (rects, options = {}) => {\n    const { color = \"red\", width: strokeWidth = 2, writingMode } = options;\n    const g = createSVGElement(\"g\");\n    g.style.fill = \"none\";\n    g.style.stroke = color;\n    g.style.strokeWidth = String(strokeWidth);\n    const block = strokeWidth * 1.5;\n    if (writingMode === \"vertical-rl\" || writingMode === \"vertical-lr\") {\n      for (const { right, top, height } of rects) {\n        const el = createSVGElement(\"path\");\n        const n = Math.max(1, Math.round(height / block / 1.5));\n        const inline = height / n;\n        const ls = Array.from({ length: n }, (_, i) => `l${i % 2 ? -block : block} ${inline}`).join(\"\");\n        el.setAttribute(\"d\", `M${right} ${top}${ls}`);\n        g.append(el);\n      }\n    } else {\n      for (const { left, bottom, width } of rects) {\n        const el = createSVGElement(\"path\");\n        const n = Math.max(1, Math.round(width / block / 1.5));\n        const inline = width / n;\n        const ls = Array.from({ length: n }, (_, i) => `l${inline} ${i % 2 ? block : -block}`).join(\"\");\n        el.setAttribute(\"d\", `M${left} ${bottom}${ls}`);\n        g.append(el);\n      }\n    }\n    return g;\n  });\n  __publicField(Overlayer, \"outline\", (rects, options = {}) => {\n    const { color = \"red\", width: strokeWidth = 3, radius = 3 } = options;\n    const g = createSVGElement(\"g\");\n    g.style.fill = \"none\";\n    g.style.stroke = color;\n    g.style.strokeWidth = String(strokeWidth);\n    for (const { left, top, height, width } of rects) {\n      const el = createSVGElement(\"rect\");\n      el.setAttribute(\"x\", String(left));\n      el.setAttribute(\"y\", String(top));\n      el.setAttribute(\"height\", String(height));\n      el.setAttribute(\"width\", String(width));\n      el.setAttribute(\"rx\", String(radius));\n      g.append(el);\n    }\n    return g;\n  });\n  // TTS cursor — same shape as highlight but applies a subtle CSS animation\n  // by class so consumers can customize the pulse without DOM churn.\n  __publicField(Overlayer, \"pulse\", (rects, options = {}) => {\n    const { color = \"currentColor\", opacity = 0.25 } = options;\n    const g = createSVGElement(\"g\");\n    g.style.fill = color;\n    g.setAttribute(\"class\", \"reader-overlay-pulse\");\n    g.style.opacity = `var(--overlay-pulse-opacity, ${opacity})`;\n    for (const { left, top, height, width } of rects) {\n      const el = createSVGElement(\"rect\");\n      el.setAttribute(\"x\", String(left));\n      el.setAttribute(\"y\", String(top));\n      el.setAttribute(\"height\", String(height));\n      el.setAttribute(\"width\", String(width));\n      g.append(el);\n    }\n    return g;\n  });\n\n  // ../../packages/shared/src/reader/textAnchor.ts\n  var ANCHOR_CONTEXT_LENGTH = 30;\n  var FUZZY_MIN_SCORE = 0.75;\n  var CONTEXT_ACCEPT_SCORE = 0.5;\n  var OFFSET_VERIFY_SCORE = 0.8;\n  function similarity(a, b) {\n    if (a === b) return 1;\n    if (a.length === 0 || b.length === 0) return 0;\n    const bigrams = (s) => {\n      const set = /* @__PURE__ */ new Set();\n      for (let i = 0; i < s.length - 1; i++) set.add(s.slice(i, i + 2));\n      return set;\n    };\n    const aBigrams = bigrams(a.toLowerCase());\n    const bBigrams = bigrams(b.toLowerCase());\n    if (aBigrams.size === 0 || bBigrams.size === 0) return 0;\n    let intersection = 0;\n    for (const bg of aBigrams) if (bBigrams.has(bg)) intersection++;\n    return 2 * intersection / (aBigrams.size + bBigrams.size);\n  }\n  function findAnchorOffset(fullText, anchor) {\n    var _a, _b;\n    if (!anchor || typeof anchor.exact !== \"string\" || anchor.exact.length === 0) return null;\n    if (typeof fullText !== \"string\" || fullText.length === 0) return null;\n    const prefix = (_a = anchor.prefix) != null ? _a : \"\";\n    const suffix = (_b = anchor.suffix) != null ? _b : \"\";\n    const withContext = findWithContext(fullText, anchor.exact, prefix, suffix);\n    if (withContext !== null) return withContext;\n    const { startOffset, endOffset } = anchor;\n    if (Number.isFinite(startOffset) && Number.isFinite(endOffset) && startOffset >= 0 && endOffset <= fullText.length && endOffset > startOffset) {\n      const atOffset = fullText.slice(startOffset, endOffset);\n      if (atOffset === anchor.exact || similarity(atOffset, anchor.exact) > OFFSET_VERIFY_SCORE) {\n        return startOffset;\n      }\n    }\n    return findFuzzyMatch(fullText, anchor.exact);\n  }\n  function findWithContext(fullText, exact, prefix, suffix) {\n    let index = fullText.indexOf(prefix + exact + suffix);\n    if (index !== -1) return index + prefix.length;\n    index = fullText.indexOf(prefix + exact);\n    if (index !== -1) return index + prefix.length;\n    index = fullText.indexOf(exact + suffix);\n    if (index !== -1) return index;\n    index = fullText.indexOf(exact);\n    if (index === -1) return null;\n    const scoreAt = (at) => similarity(fullText.slice(Math.max(0, at - ANCHOR_CONTEXT_LENGTH), at), prefix) + similarity(fullText.slice(at + exact.length, at + exact.length + ANCHOR_CONTEXT_LENGTH), suffix);\n    const first = scoreAt(index);\n    if (first >= CONTEXT_ACCEPT_SCORE * 2 || similarity(fullText.slice(Math.max(0, index - ANCHOR_CONTEXT_LENGTH), index), prefix) > CONTEXT_ACCEPT_SCORE || similarity(fullText.slice(index + exact.length, index + exact.length + ANCHOR_CONTEXT_LENGTH), suffix) > CONTEXT_ACCEPT_SCORE) {\n      return index;\n    }\n    let bestIndex = index;\n    let bestScore = first;\n    let searchFrom = index + 1;\n    while ((index = fullText.indexOf(exact, searchFrom)) !== -1) {\n      const score = scoreAt(index);\n      if (score > bestScore) {\n        bestScore = score;\n        bestIndex = index;\n      }\n      searchFrom = index + 1;\n    }\n    return bestIndex;\n  }\n  function findFuzzyMatch(fullText, exact) {\n    if (exact.length >= 100) return null;\n    let bestIndex = -1;\n    let bestScore = FUZZY_MIN_SCORE;\n    for (let i = 0; i <= fullText.length - exact.length; i++) {\n      const score = similarity(fullText.slice(i, i + exact.length), exact);\n      if (score > bestScore) {\n        bestScore = score;\n        bestIndex = i;\n      }\n    }\n    return bestIndex === -1 ? null : bestIndex;\n  }\n\n  // ../../packages/shared/src/reader/textPosition.ts\n  var TEXT_POSITION_VERSION = 1;\n  var MAX_SERIALISED_LENGTH = 4096;\n  function parseTextPosition(json) {\n    if (typeof json !== \"string\" || json.length === 0 || json.length > MAX_SERIALISED_LENGTH) return null;\n    let raw;\n    try {\n      raw = JSON.parse(json);\n    } catch (e) {\n      return null;\n    }\n    if (!raw || typeof raw !== \"object\") return null;\n    const p = raw;\n    if (p.v !== TEXT_POSITION_VERSION) return null;\n    if (typeof p.chapterSlug !== \"string\" || p.chapterSlug.length === 0) return null;\n    const a = p.anchor;\n    if (!a || typeof a.exact !== \"string\" || a.exact.length === 0) return null;\n    return {\n      v: TEXT_POSITION_VERSION,\n      chapterSlug: p.chapterSlug,\n      anchor: {\n        prefix: typeof a.prefix === \"string\" ? a.prefix : \"\",\n        exact: a.exact,\n        suffix: typeof a.suffix === \"string\" ? a.suffix : \"\",\n        startOffset: clampInt(a.startOffset, 0, Number.MAX_SAFE_INTEGER),\n        endOffset: clampInt(a.endOffset, 0, Number.MAX_SAFE_INTEGER)\n      },\n      charOffset: clampInt(p.charOffset, 0, Number.MAX_SAFE_INTEGER),\n      chapterFraction: clampUnit(p.chapterFraction)\n    };\n  }\n  function resolveTextPosition(pos, chapterSlug, chapterText) {\n    if (!pos || !chapterSlug || pos.chapterSlug !== chapterSlug) return null;\n    if (typeof chapterText === \"string\" && chapterText.length > 0) {\n      const at = findAnchorOffset(chapterText, pos.anchor);\n      if (at !== null) return { kind: \"anchor\", offset: nearestOccurrence(chapterText, pos, at) };\n    }\n    if (pos.chapterFraction > 0) return { kind: \"fraction\", fraction: pos.chapterFraction };\n    return null;\n  }\n  function nearestOccurrence(text, pos, resolved) {\n    const exact = pos.anchor.exact;\n    let index = text.indexOf(exact);\n    if (index === -1) return resolved;\n    const second = text.indexOf(exact, index + 1);\n    if (second === -1) return resolved;\n    let best = resolved;\n    let bestDistance = Math.abs(resolved - pos.charOffset);\n    for (; index !== -1; index = text.indexOf(exact, index + 1)) {\n      const distance = Math.abs(index - pos.charOffset);\n      if (distance < bestDistance) {\n        bestDistance = distance;\n        best = index;\n      }\n    }\n    return best;\n  }\n  function clampInt(n, min, max) {\n    if (typeof n !== \"number\" || !Number.isFinite(n)) return min;\n    return Math.max(min, Math.min(max, Math.round(n)));\n  }\n  function clampUnit(n) {\n    if (typeof n !== \"number\" || !Number.isFinite(n)) return 0;\n    return Math.max(0, Math.min(1, n));\n  }\n\n  // ../../packages/reader-overlay/src/mobileBootstrap.ts\n  function adapt(ov) {\n    return {\n      element: ov.element,\n      add: (key, range, draw, options) => ov.add(key, range, draw, options),\n      remove: (key) => ov.remove(key),\n      clear: () => ov.clear(),\n      redraw: () => ov.redraw(),\n      syncScroll: () => ov.syncScroll(),\n      hitTest: (point) => ov.hitTest(point),\n      size: () => ov.size,\n      markJustAnchored: (ms) => ov.markJustAnchored(ms),\n      isJustAnchored: () => ov.isJustAnchored()\n    };\n  }\n  if (typeof window !== \"undefined\" && !window.__TSAnchor) {\n    window.__TSAnchor = {\n      findOffset: findAnchorOffset,\n      resolvePosition: (json, chapterSlug, chapterText) => resolveTextPosition(parseTextPosition(json), chapterSlug, chapterText)\n    };\n  }\n  if (typeof window !== \"undefined\" && !window.__TSOverlayer) {\n    window.__TSOverlayer = {\n      create: () => adapt(new Overlayer()),\n      highlight: Overlayer.highlight,\n      underline: Overlayer.underline,\n      outline: Overlayer.outline,\n      strikethrough: Overlayer.strikethrough,\n      squiggly: Overlayer.squiggly,\n      pulse: Overlayer.pulse\n    };\n  }\n})();\n"
