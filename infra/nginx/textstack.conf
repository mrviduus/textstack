# Textstack Production Nginx Configuration
# Install: sudo cp this_file /etc/nginx/sites-available/textstack
#          sudo ln -s /etc/nginx/sites-available/textstack /etc/nginx/sites-enabled/
#          sudo nginx -t && sudo systemctl reload nginx

# Rate limiting for API
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=upload_limit:10m rate=1r/s;

# Upstream for API (Docker container on localhost only)
upstream textstack_api {
    server 127.0.0.1:8080;
    keepalive 32;
}

# Upstream for Admin panel (Docker container on localhost only)
upstream textstack_admin {
    server 127.0.0.1:5174;
}

# --- textstack.app (General books site) ---
server {
    listen 80;
    listen [::]:80;
    server_name textstack.app www.textstack.app localhost;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect to HTTPS (uncomment after SSL is set up)
    # location / {
    #     return 301 https://$server_name$request_uri;
    # }

    # Temporary: serve directly until SSL is ready
    root /home/vasyl/projects/onlinelib/onlinelib/apps/web/dist;
    index _nonexistent_;  # Disable default index - SPA uses try_files fallback

    # 301 redirect legacy URLs without language prefix to /en/*
    location ~ ^/(books|authors|genres|search|about|library)(/.*)?$ {
        return 301 /en$uri;
    }

    # Homepage redirect to default language
    location = / {
        return 301 /en/;
    }

    # SEO files - proxy to API
    location = /robots.txt {
        proxy_pass http://textstack_api/robots.txt;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Site-Id general;
    }

    location = /sitemap.xml {
        proxy_pass http://textstack_api/sitemap.xml;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Site-Id general;
    }

    location /sitemaps/ {
        proxy_pass http://textstack_api/sitemaps/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Site-Id general;
    }

    # API proxy
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;

        proxy_pass http://textstack_api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Site-Id general;
        proxy_set_header Connection "";

        # File uploads
        client_max_body_size 100M;
        proxy_read_timeout 300s;
    }

    # Static files for uploads/covers
    location /storage/ {
        alias /home/vasyl/projects/onlinelib/onlinelib/data/storage/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # SSG pages - serve pre-rendered HTML if exists
    # Homepage
    location ~ ^/(en|uk)/?$ {
        add_header X-SEO-Render "ssg" always;
        try_files /ssg$uri/index.html @spa;
    }

    # Book detail pages
    location ~ ^/(en|uk)/books/[^/]+/?$ {
        add_header X-SEO-Render "ssg" always;
        try_files /ssg$uri/index.html @spa;
    }

    # Author detail pages
    location ~ ^/(en|uk)/authors/[^/]+/?$ {
        add_header X-SEO-Render "ssg" always;
        try_files /ssg$uri/index.html @spa;
    }

    # Genre detail pages
    location ~ ^/(en|uk)/genres/[^/]+/?$ {
        add_header X-SEO-Render "ssg" always;
        try_files /ssg$uri/index.html @spa;
    }

    # List pages (books, authors, genres, about)
    location ~ ^/(en|uk)/(books|authors|genres|about)/?$ {
        add_header X-SEO-Render "ssg" always;
        try_files /ssg$uri/index.html @spa;
    }

    # SPA fallback for non-SSG routes
    location @spa {
        add_header X-SEO-Render "spa" always;
        add_header X-Robots-Tag "noindex, follow" always;
        try_files $uri /index.html;
    }

    # Default - SPA
    location / {
        add_header X-SEO-Render "spa" always;
        add_header X-Robots-Tag "noindex, follow" always;
        try_files $uri /index.html;
    }

    # Static assets caching
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 1000;
}

# --- textstack.dev (Admin Panel - auth required) ---
server {
    listen 80;
    listen [::]:80;
    server_name textstack.dev;

    # Block indexing - admin-only domain, no public content
    add_header X-Robots-Tag "noindex, nofollow" always;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect to HTTPS (uncomment after SSL is set up)
    # location / {
    #     return 301 https://$server_name$request_uri;
    # }

    # robots.txt - disallow everything
    location = /robots.txt {
        return 200 "User-agent: *\nDisallow: /\n";
        add_header Content-Type text/plain;
    }

    # API proxy for admin endpoints
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;

        proxy_pass http://textstack_api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Site-Id general;  # Admin works with general site
        proxy_set_header Connection "";

        # File uploads for admin
        client_max_body_size 100M;
        proxy_read_timeout 300s;
    }

    # Static files for uploads/covers (admin needs to see book covers)
    location /storage/ {
        alias /home/vasyl/projects/onlinelib/onlinelib/data/storage/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Proxy to admin SPA (Vite dev server in Docker)
    location / {
        proxy_pass http://textstack_admin;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 1000;
}

# --- HTTPS servers (uncomment after certbot runs) ---

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name textstack.app www.textstack.app;
#
#     ssl_certificate /etc/letsencrypt/live/textstack.app/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/textstack.app/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     # Security headers
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     root /home/vasyl/projects/onlinelib/onlinelib/apps/web/dist;
#     index _nonexistent_;
#
#     location /api/ {
#         limit_req zone=api_limit burst=20 nodelay;
#         proxy_pass http://textstack_api/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Site-Id general;
#         proxy_set_header Connection "";
#         client_max_body_size 100M;
#         proxy_read_timeout 300s;
#     }
#
#     location /storage/ {
#         alias /home/vasyl/projects/onlinelib/onlinelib/data/storage/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location / {
#         add_header X-SEO-Render "static" always;
#         try_files $uri /index.html;
#     }
#
#     location /assets/ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
#     gzip_min_length 1000;
# }

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name textstack.dev;

    ssl_certificate /etc/letsencrypt/live/textstack.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/textstack.dev/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Block indexing - admin-only domain
    add_header X-Robots-Tag "noindex, nofollow" always;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # robots.txt - disallow everything
    location = /robots.txt {
        return 200 "User-agent: *\nDisallow: /\n";
        add_header Content-Type text/plain;
    }

    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;
        proxy_pass http://textstack_api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Site-Id general;
        proxy_set_header Connection "";
        client_max_body_size 100M;
        proxy_read_timeout 300s;
    }

    location /storage/ {
        alias /home/vasyl/projects/onlinelib/onlinelib/data/storage/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Proxy to admin SPA
    location / {
        proxy_pass http://textstack_admin;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    gzip_min_length 1000;
}
