{
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=books;Username=app;Password=changeme"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "textstack.app;textstack.dev;localhost;*.localhost;api",
  "Jwt": {
    "SecretKey": "",
    "Issuer": "textstack.app",
    "AccessTokenExpiryMinutes": 60,
    "RefreshTokenExpiryDays": 365,
    "GuestRefreshTokenExpiryDays": 30
  },
  "Google": {
    "ClientId": ""
  },
  "OpenAI": {
    "ApiKey": "",
    "Model": "gpt-4.1-nano",
    "Explain": {
      "Model": "gpt-4.1-mini"
    },
    "RagAsk": {
      "Model": "gpt-4.1"
    },
    "Pdf": {
      "Model": "gpt-4.1"
    },
    "Embedding": {
      "Model": "text-embedding-3-small"
    },
    "Translate": {
      "MaxTextLength": 500
    }
  },
  "Eval": {
    "JudgeModel": "gpt-4.1",
    "Scheduled": {
      "__comment": "OFF by default; spends judge $ when enabled (ContinuousEvalWorker, slice 5a).",
      "Enabled": false,
      "CheckIntervalHours": 1,
      "IntervalHours": 24,
      "Features": [ "explain" ],
      "RegressionDrop": 0.5,
      "DailyBudgetUsd": 0
    }
  },
  "Drift": {
    "__comment": "OFF by default; spends embedding $ when enabled (DriftDetectionWorker, slice 5b).",
    "Enabled": false,
    "CheckIntervalHours": 1,
    "IntervalHours": 24,
    "Features": [ "explain" ],
    "MaxSampleSize": 50,
    "MinSampleSize": 10,
    "Threshold": 0.15,
    "ConsecutiveDays": 2,
    "BaselineWindowDays": 7
  },
  "Translate": {
    "CachePath": "data/translate-cache",
    "CacheTtlDays": 30
  },
  "Explain": {
    "MaxWordLength": 100,
    "MaxSentenceLength": 800,
    "CachePath": "data/explain-cache",
    "CacheTtlDays": 30
  },
  "Tts": {
    "CachePath": "data/tts-cache",
    "MaxTextLength": 500,
    "TimeoutSeconds": 15
  },
  "_DictionaryComment": "Word tap in the reader — fast or useless, so TimeoutSeconds is 3, not the 10 that turned the 2026-09-05 api.dictionaryapi.dev outage into a ten-second spinner ending in 504. CacheTtlDays is FRESHNESS only: past it we revalidate, but the entry is kept and served (flagged stale) when upstream is unreachable, which is what makes an upstream outage a non-event for any word already looked up. NegativeCacheTtlHours is much shorter because 'no such word' is a claim about coverage and dictionaries gain words. CacheMaxAgeDays is the separate retention bound the sweeper enforces.",
  "Dictionary": {
    "TimeoutSeconds": 3,
    "CachePath": "data/dictionary-cache",
    "CacheTtlDays": 30,
    "NegativeCacheTtlHours": 24,
    "CacheMaxAgeDays": 365
  },
  "Ollama": {
    "BaseUrl": "http://localhost:11434",
    "Model": "gemma4:e2b",
    "TimeoutSeconds": 90
  },
  "LLM": {
    "DefaultProvider": "openai",
    "Providers": {
      "Explain": "openai",
      "Translate": "openai",
      "Distractor": "ollama",
      "BookMetadata": "ollama",
      "TagSuggestion": "ollama"
    }
  },
  "Ai": {
    "DefaultProvider": "openai",
    "Routes": {
      "explain": "openai-explain",
      "explain.toolcall": "openai-explain",
      "translate": "openai",
      "distractor": "ollama",
      "bookmeta": "ollama",
      "bookmeta.agent": "openai-explain",
      "tutor.agent": "openai-explain",
      "tagsuggestion": "ollama",
      "podcast.script": "openai",
      "rag.ask": "openai-rag",
      "pdf.parse": "openai-pdf",
      "rag.summarize": "openai"
    },
    "_PdfComment": "ADR-012 S3: user-PDF vision→Markdown RAG parse. Ai:Pdf:MaxParsePages caps pages parsed (beyond it → partial index).",
    "Pdf": {
      "MaxParsePages": 300
    },
    "Tracing": {
      "Sampling": {
        "Default": 1.0,
        "PerFeature": {
          "explain": 0.1,
          "translate": 0.1
        }
      }
    },
    "_ShadowComment": "Shadow routing is OFF by default: shadow makes PAID background LLM calls. Enable per-feature by adding a Routes entry (feature -> shadow provider key, e.g. 'openai-explain') AND a SampleRates entry (feature -> 0..1). Default:SampleRate applies to any routed feature without an explicit SampleRates entry.",
    "Shadow": {
      "Default": {
        "SampleRate": 0.0
      },
      "Routes": {},
      "SampleRates": {},
      "TimeoutSeconds": 15
    },
    "_BudgetsComment": "Per-feature daily (UTC) USD budget. OFF by default (Default:Mode=off + empty Features). Enable per feature: Features:{feature}:{DailyUsd (>0), Fallback (cheaper provider key, e.g. 'ollama'), Mode ('fallback'|'hardstop')}. fallback reroutes over budget; hardstop returns HTTP 429. Spend is process-local + UTC-daily, lazy-seeded from sampled llm_traces on restart (approximate).",
    "Budgets": {
      "Default": {
        "Mode": "off"
      },
      "Features": {}
    },
    "_RouteAlarmComment": "Sentry warning when one of these tasks resolves via Ai:DefaultProvider instead of an explicit route — the 2026-07-14 shape (pdf.parse silently fell to Ollama and pegged the CPU at ~390%). Throttled per (task, provider): a 106-page PDF raises one event, not 106. The same cooldown throttles provider failure/timeout alarms.",
    "RouteAlarm": {
      "AlertOnDefaultRouteFor": [ "pdf.parse", "rag.summarize", "podcast.script" ],
      "CooldownMinutes": 60
    }
  },
  "Sentry": {
    "_comment": "DSN comes from the SENTRY_DSN env var only — never commit it. Unset ⇒ Sentry is a complete no-op (no SDK init, no middleware). TracesSampleRate applies to http.server transactions; ai.agent/rag.index always sample at 1.0 and /health never samples.",
    "TracesSampleRate": 0.2
  },
  "_RateLimitsComment": "Only the rate-limiter values a deployment has to move. Everything else is a literal in AddTextStackRateLimiting next to the comment that justifies it. GuestSessionPermitLimit is guest-creates per IP per 5-minute window; production keeps 3, CI raises it via RateLimits__GuestSessionPermitLimit because the guest-merge suite needs >=6 guests from one host. It is a knob, not a bypass: the policy, window and partition key are the same code in CI and in production. A configured <=0 degrades to 3 rather than locking everyone out.",
  "RateLimits": {
    "GuestSessionPermitLimit": 3,
    "UserLoginPermitLimit": 10,
    "ClipPermitLimit": 20,
    "AccountDeletePermitLimit": 3,
    "RagAskPermitLimit": 30
  },
  "_EntitlementsComment": "Per-tier quotas. The tier is stored on users.tier (Guest|Free|Supporter|Staff). Staff is additionally granted by StaffEmails — but NEVER to a guest (guest emails are synthesized, so a collision must not mint a 5GB anonymous session). A user row may carry storage_limit_override_bytes, which wins over the tier and is clamped to MaxStorageLimitBytes. MaxSingleUploadBytes is a PLATFORM constraint, not a tier perk: Cloudflare caps one request body at ~100MB (verified — 90MB reached the API, 110MB returned 413 cloudflare), so it is deliberately NOT expressible per tier and config may only LOWER it. Bigger files must use the chunked upload endpoints. An unknown tier key, or an absent/<=0 value, falls back to Default and then to a compiled-in 500MB — this section can never brick uploads. DailyEnrichmentCap is new-vocabulary-words-per-UTC-day that a tier may push through paid LLM enrichment (distractors+hint+explanation fire on every word save). Only Guest sets it: /auth/guest + word saving is otherwise an account-less path to unmetered paid inference. 50/day is an honest first encounter, not a farm. Absent or <=0 means unlimited, same as MaxBooks. AiEnabled is the ON/OFF switch above that cap: false closes the whole paid-inference surface (tutor) for the tier, server-side, with 403 account_required. Only Guest sets it. Unset means allowed — a missing key must cost us a call, not silently kill AI for paying users.",
  "Entitlements": {
    "Default": { "StorageLimitBytes": 524288000, "MaxBooks": null },
    "Tiers": {
      "Guest": { "StorageLimitBytes": 52428800, "MaxBooks": 1, "DailyEnrichmentCap": 50, "AiEnabled": false },
      "Free": { "StorageLimitBytes": 524288000, "MaxBooks": null },
      "Supporter": { "StorageLimitBytes": 2147483648, "MaxBooks": null },
      "Staff": { "StorageLimitBytes": 5368709120, "MaxBooks": null }
    },
    "StaffEmails": [],
    "MaxSingleUploadBytes": 83886080,
    "MaxStorageLimitBytes": 21474836480
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5173",
      "http://general.localhost",
      "http://general.localhost:5173",
      "http://localhost:81",
      "http://admin.localhost",
      "http://admin.localhost:81",
      "https://textstack.app",
      "https://textstack.dev"
    ]
  }
}
