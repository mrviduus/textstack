# Production Docker Compose Configuration
# Usage: docker compose -f docker-compose.prod.yml --env-file .env.production up -d
#
# Key differences from development:
# - No nginx (using host nginx)
# - No frontend dev servers (using static files)
# - PostgreSQL not exposed externally
# - Restart policies enabled
# - Production environment variables
# - Resource limits

services:
  db:
    image: postgres:16
    container_name: textstack_db_prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ./data/postgres-prod:/var/lib/postgresql/data
    # NOTE: No ports exposed - internal only for security
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M

  migrator:
    build:
      context: ./backend
      dockerfile: Docker/Migrator.Dockerfile
    container_name: textstack_migrator_prod
    environment:
      ConnectionStrings__Default: "Host=db;Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
    depends_on:
      db:
        condition: service_healthy
    restart: "no"

  api:
    build:
      context: ./backend
      dockerfile: Docker/Api.Dockerfile
    container_name: textstack_api_prod
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ConnectionStrings__Default: "Host=db;Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
      Storage__RootPath: /storage
      Jwt__SecretKey: ${JWT_SECRET}
      Jwt__Issuer: ${JWT_ISSUER}
      Jwt__Audience: ${JWT_AUDIENCE}
      Google__ClientId: ${GOOGLE_CLIENT_ID}
      OTEL_EXPORTER_OTLP_ENDPOINT: http://aspire-dashboard:18890
    volumes:
      - ./data/storage:/storage
      - ./data/textstack:/data/textstack
    ports:
      - "127.0.0.1:8080:8080"  # Only localhost, nginx will proxy
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    depends_on:
      db:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      aspire-dashboard:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M

  worker:
    build:
      context: ./backend
      dockerfile: Docker/Worker.Dockerfile
    container_name: textstack_worker_prod
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ConnectionStrings__Default: "Host=db;Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
      Storage__RootPath: /storage
      OTEL_EXPORTER_OTLP_ENDPOINT: http://aspire-dashboard:18890
    volumes:
      - ./data/storage:/storage
    restart: always
    depends_on:
      db:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      aspire-dashboard:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 2G  # Worker needs more for book processing
        reservations:
          memory: 512M

  admin:
    build:
      context: ./apps/admin
      dockerfile: Dockerfile
    container_name: textstack_admin_prod
    environment:
      VITE_API_URL: /api
    ports:
      - "127.0.0.1:5174:5174"  # Only localhost - not exposed to internet
    restart: always
    depends_on:
      - api
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

  # Prerender for SEO - renders SPA for search engine bots
  prerender:
    image: tvanro/prerender-alpine:7.2.0
    container_name: textstack_prerender_prod
    environment:
      MEMORY_CACHE: 1
      CACHE_MAXSIZE: 1000
      CACHE_TTL: 3600
    ports:
      - "127.0.0.1:3030:3000"  # Only localhost, nginx will proxy
    restart: always
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M

  # ===========================================
  # Observability - Aspire Dashboard (all-in-one: traces, logs, metrics)
  # ===========================================

  aspire-dashboard:
    image: mcr.microsoft.com/dotnet/aspire-dashboard:latest
    container_name: textstack_aspire_prod
    ports:
      - "127.0.0.1:18888:18888"   # Dashboard UI (localhost only)
    environment:
      - ASPIRE_DASHBOARD_UNSECURED_ALLOW_ANONYMOUS=true
      - DASHBOARD__TELEMETRYLIMITS__MAXLOGCOUNT=100000
      - DASHBOARD__TELEMETRYLIMITS__MAXTRACECOUNT=50000
      - DASHBOARD__TELEMETRYLIMITS__MAXMETRICCOUNT=100000
    restart: always
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M
